{
  "@context": {
    "vitrine": "https://w3id.org/vitrine/ns#",
    "rdfs": "http://www.w3.org/2000/01/rdf-schema#",
    "label": {
      "@id": "rdfs:label",
      "@language": "en"
    },
    "comment": {
      "@id": "rdfs:comment",
      "@language": "en"
    },
    "note": {
      "@id": "rdfs:comment",
      "@language": "en"
    }
  },
  "@id": "https://aleph.garden/views/markdown",
  "@type": "vitrine:View",
  "label": "Markdown view",
  "comment": "Obsidian-flavoured Markdown: frontmatter as properties, wikilinks resolved through the pod's type index, embeds, tags, tasks, callouts, math, diagrams, and SPARQL blocks run through the host.",
  "note": "Ships inside the host bundle. It is compiled into the hosts that carry it, and no module exists to fetch, so this descriptor names no implementation. A host whose bundle lacks this view cannot obtain it from this document."
}
