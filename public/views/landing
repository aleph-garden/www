{
  "@context": {
    "vitrine": "https://w3id.org/vitrine/ns#",
    "rdfs": "http://www.w3.org/2000/01/rdf-schema#",
    "label": {
      "@id": "rdfs:label",
      "@language": "en"
    },
    "comment": {
      "@id": "rdfs:comment",
      "@language": "en"
    },
    "note": {
      "@id": "rdfs:comment",
      "@language": "en"
    }
  },
  "@id": "https://aleph.garden/views/landing",
  "@type": "vitrine:View",
  "label": "Landing view",
  "comment": "What a host shows at its own address, where there is no resource to read. The rule that selects it matches the deployment's own IRI, so a preview deployment and a local run greet as well.",
  "note": "Ships inside the host bundle. It is compiled into the hosts that carry it, and no module exists to fetch, so this descriptor names no implementation. A host whose bundle lacks this view cannot obtain it from this document."
}
