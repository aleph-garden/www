---
// Starlight's footer slot, inside the content column, keeps only the
// previous and next links. The site footer runs across the whole viewport
// below the frame, so PageFrame.astro draws it.
import Pagination from '@astrojs/starlight/components/Pagination.astro'
---

<Pagination />
